# Heaven Develop Environment

* git global config file

	~/.gitconfig

* git ignore add ignore list to global config

	git config --global core.excludesfile ~/.gitignore

* if ssh key permission problem

	$ chmod -v -R 600 id_rsa

